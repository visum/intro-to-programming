BASE.require([
    "jQuery"
], function () {
    BASE.namespace("components.ui.inputs");

    components.ui.inputs.UIRadio = function (elem, tags) {
        var self = this;
        var $elem = $(elem);

    };
});